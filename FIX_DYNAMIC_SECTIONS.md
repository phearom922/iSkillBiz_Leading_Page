# Fix Dynamic Sections - แก้ไขให้ใช้ Dynamic Sections

## ปัญหาที่พบ

1. **หน้าเว็บยังใช้ components เดิม**: `app/page.tsx` ยังใช้ components เดิมทั้งหมด (WhyIskillsbiz, Differentiation, PainPoints, etc.) แทนที่จะใช้ `DynamicSections`
2. **ไม่มีการดึงข้อมูลจาก database**: หน้าเว็บไม่แสดง sections ที่สร้างจาก admin dashboard
3. **ไม่มีการจัดเรียงตาม order**: Sections ไม่ถูกจัดเรียงตาม order ที่กำหนดใน admin dashboard

## การแก้ไข

### 1. แก้ไข `app/page.tsx`

เปลี่ยนจาก:
```tsx
// ใช้ components เดิมทั้งหมด
<WhyIskillsbiz />
<Differentiation />
<PainPoints />
// ... etc
```

เป็น:
```tsx
// ใช้ DynamicSections ที่ดึงข้อมูลจาก database
<DynamicSections />
```

### 2. โครงสร้างใหม่

```
app/page.tsx
├── Header
├── Hero (component เดิม)
├── DynamicSections (ดึงจาก database)
│   ├── Section 1 (ตาม order)
│   ├── Section 2 (ตาม order)
│   └── ...
└── Pricing (component เดิม)
└── Footer
```

### 3. การทำงาน

- `DynamicSections` จะ:
  1. ดึง sections จาก `ContentProvider`
  2. กรองเฉพาะ sections ที่ `is_active = true`
  3. เรียงตาม `order` (น้อยไปมาก)
  4. กรองออก sections พิเศษ (Hero Section, Pricing)
  5. แสดงแต่ละ section ด้วย `DynamicSection` component

- `DynamicSection` จะ:
  1. แสดง Title + Description (ถ้ามี)
  2. แสดง YouTube Videos (grid ตามจำนวน)
  3. แสดง Images (grid 3 columns)

## วิธีใช้งาน

### เพิ่ม Section ใหม่

1. ไปที่ `/admin/sections`
2. คลิก "Add Section"
3. ใส่ข้อมูล:
   - **Name**: ชื่อ section
   - **Type**: text, hero, youtube, image
   - **Order**: ลำดับการแสดง (ตัวเลขน้อยแสดงก่อน)
4. คลิก "Create Section"

### จัดการ Content

1. คลิก "Edit" บน section ที่ต้องการ
2. แก้ไขใน 3 tabs:
   - **Title & Description**: ใส่ข้อความ
   - **YouTube Videos**: เพิ่ม/ลบ videos
   - **Images**: อัปโหลด/ลบ images
3. คลิก "Save Changes"

### จัดเรียง Sections

- ใช้ field **Order** ใน admin dashboard
- ตัวเลขน้อยแสดงก่อน
- สามารถแก้ไข order ได้โดยแก้ไข section

## หมายเหตุ

- **Hero Section** และ **Pricing** ยังใช้ components เดิม (ไม่ใช้ DynamicSection)
- Sections อื่นๆ จะแสดงตาม order ที่กำหนด
- ถ้า section ไม่มี content ใดๆ จะไม่แสดงบนหน้าเว็บ
- Sections จะอัปเดตอัตโนมัติเมื่อมีการเปลี่ยนแปลงใน database

## Debug

ถ้า sections ไม่แสดง:

1. เช็ค console logs:
   - `📋 DynamicSections: Found X sections to display`
   - `📋 Sections order: ...`
2. เช็คว่า sections มี `is_active = true`
3. เช็คว่า sections มี content (title, description, videos, หรือ images)
4. เช็คว่า Supabase configuration ถูกต้อง (`.env.local`)


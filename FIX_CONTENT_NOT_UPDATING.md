# Fix: หน้า Landing Page ไม่แสดงการเปลี่ยนแปลงหลังจากแก้ไขใน Admin Dashboard

## ปัญหา

เมื่อแก้ไขข้อมูลใน Admin Dashboard และ Save สำเร็จแล้ว แต่หน้า Landing Page (หน้าบ้าน) ไม่มีการเปลี่ยนแปลง

## สาเหตุที่เป็นไปได้

1. **Realtime Subscription ไม่ทำงาน**: ถ้า Supabase environment variables ไม่ได้ตั้งค่า หรือ realtime ไม่ได้เปิดใช้งาน
2. **Browser Cache**: Browser อาจ cache ข้อมูลเก่า
3. **API Route ไม่ได้ทำงาน**: `/api/content` อาจไม่ได้ fetch ข้อมูลจาก database

## วิธีแก้ไข

### 1. ตรวจสอบ Console Logs

เปิด Browser Developer Tools (F12) และดู Console:

- ควรเห็น: `🔄 Fetching content from /api/content...`
- ควรเห็น: `✅ Content fetched: {...}`
- ถ้าเห็น: `⚠️ No sections found` หรือ `⚠️ Section not found` แสดงว่า API ไม่ได้ fetch ข้อมูล

### 2. ตรวจสอบ Environment Variables

ตรวจสอบว่าไฟล์ `.env.local` มี Supabase credentials:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

### 3. Refresh หน้าเว็บ

**วิธีที่ง่ายที่สุด**: กด `Ctrl + Shift + R` (Windows) หรือ `Cmd + Shift + R` (Mac) เพื่อ hard refresh

หรือ:
- กด `F5` เพื่อ refresh
- หรือปิดและเปิด browser tab ใหม่

### 4. ตรวจสอบ Network Tab

1. เปิด Browser Developer Tools (F12)
2. ไปที่ tab "Network"
3. Refresh หน้าเว็บ
4. หา request ไปที่ `/api/content`
5. ตรวจสอบว่า response มีข้อมูลหรือไม่

### 5. ตรวจสอบว่า Backend ทำงาน

ตรวจสอบว่า NestJS backend ทำงานอยู่ที่ port 3001:

```bash
cd backend
npm run start:dev
```

### 6. ตรวจสอบ Supabase Database

ตรวจสอบว่า:
1. ข้อมูลถูกบันทึกใน Supabase database หรือไม่
2. `sections` table มีข้อมูล
3. `section_content` table มีข้อมูลที่ถูกต้อง

## การแก้ไขที่ทำไปแล้ว

1. ✅ เพิ่ม console.log เพื่อ debug
2. ✅ เพิ่ม polling mechanism (ทุก 30 วินาที) เพื่อ refresh ข้อมูลอัตโนมัติ
3. ✅ เพิ่ม `cache: "no-store"` เพื่อป้องกัน browser cache
4. ✅ เพิ่ม `refetch` function ใน ContentProvider

## วิธีทดสอบ

1. เปิดหน้า Landing Page (`http://localhost:3000`)
2. เปิด Browser Developer Tools (F12) → Console tab
3. แก้ไขข้อมูลใน Admin Dashboard
4. ดู Console logs:
   - ควรเห็น `📡 Realtime update on section_content: {...}` (ถ้า realtime ทำงาน)
   - หรือรอ 30 วินาทีเพื่อให้ polling ทำงาน
5. ตรวจสอบว่าหน้าเว็บแสดงข้อมูลใหม่

## หมายเหตุ

- **Polling Interval**: ตั้งไว้ที่ 30 วินาที เพื่อไม่ให้โหลด server มากเกินไป
- **Realtime**: ถ้า Supabase realtime ทำงาน จะ update ทันทีเมื่อมีการเปลี่ยนแปลง
- **Manual Refresh**: ถ้าไม่เห็นการเปลี่ยนแปลง ให้ refresh หน้าเว็บ (F5 หรือ Ctrl+Shift+R)

## ถ้ายังไม่ทำงาน

1. ตรวจสอบ Console logs ใน Browser
2. ตรวจสอบ Network tab ว่า `/api/content` return ข้อมูลหรือไม่
3. ตรวจสอบ Supabase dashboard ว่าข้อมูลถูกบันทึกหรือไม่
4. ตรวจสอบ environment variables ใน `.env.local`


# ⚠️ สำคัญมาก! สร้างไฟล์ `.env.local` ใน Root Directory

## 🔴 ปัญหา

คุณมีไฟล์ `.env.local` ใน `backend/` directory แล้ว แต่ **Next.js frontend ต้องการไฟล์ `.env.local` ใน root directory** (ที่เดียวกับ `package.json`)

## ✅ วิธีแก้ไข

### ขั้นตอนที่ 1: Copy ไฟล์จาก backend/.env.local

1. เปิดไฟล์ `backend/.env.local` ใน VS Code
2. **Copy เนื้อหาทั้งหมด** (Ctrl + A, Ctrl + C)
3. สร้างไฟล์ใหม่ชื่อ `.env.local` ใน **root directory** (ที่เดียวกับ `package.json`)
4. **Paste เนื้อหา** (Ctrl + V)

### ขั้นตอนที่ 2: ตรวจสอบเนื้อหา

ไฟล์ `.env.local` ใน root directory ควรมี:

```env
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://ggtpjagiuilitxpevoti.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:3001
```

**หมายเหตุ:**
- `SUPABASE_SERVICE_ROLE_KEY` ไม่จำเป็นสำหรับ frontend (ใช้เฉพาะ backend)
- `NESTJS_PORT`, `JWT_SECRET`, `FRONTEND_URL` ไม่จำเป็นสำหรับ frontend

### ขั้นตอนที่ 3: Restart Next.js Server

**สำคัญมาก!** หลังจากสร้างไฟล์:

1. **หยุด Next.js server** (กด `Ctrl + C` ใน terminal ที่รัน `npm run dev`)
2. **Start ใหม่**: `npm run dev`
3. **Refresh หน้าเว็บ** (กด `Ctrl + Shift + R`)

## 🔍 ตรวจสอบว่าแก้ไขสำเร็จ

หลังจาก restart แล้ว ดู terminal logs:

**✅ สำเร็จ:**
- ไม่เห็น `⚠️ Supabase environment variables are missing`
- เห็น `✅ Content fetched: X sections` ใน browser console

**❌ ยังไม่สำเร็จ:**
- ยังเห็น `⚠️ Supabase environment variables are missing`
- ตรวจสอบว่า:
  1. ไฟล์ `.env.local` อยู่ใน root directory (ที่เดียวกับ `package.json`)
  2. ชื่อไฟล์ถูกต้อง (มีจุด `.` ด้านหน้า)
  3. Restart server แล้ว

## 📁 โครงสร้างไฟล์ที่ถูกต้อง

```
Static_SaaS_Landing_Page/
├── .env.local          ← ไฟล์นี้ (สำหรับ Next.js frontend)
├── package.json
├── backend/
│   └── .env.local      ← ไฟล์นี้ (สำหรับ NestJS backend)
└── ...
```

## 🚨 ถ้ายังไม่ทำงาน

1. ตรวจสอบว่าไฟล์ `.env.local` อยู่ใน root directory
2. ตรวจสอบว่าไม่มี typo ในชื่อตัวแปร
3. ตรวจสอบว่า Supabase URL และ Key ถูกต้อง
4. **Restart server** (สำคัญมาก!)

